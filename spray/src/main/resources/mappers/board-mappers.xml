<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sprayou.spray.mapper.BoardMapper">

    <select id="list" parameterType="String" resultType="boardDto">
        SELECT no
            , title
            , writer
            , regdte
            , updte
            , readcnt
            , productCd
        FROM boardList_tb
       <choose>
        <when test='type != null'>
        WHERE board_type = #{type}
        </when>
       </choose> 
       LIMIT 10
    </select>

    <select id="detail" parameterType="String" resultType="boardDto">
        SELECT
        t0.*
        FROM
        boardList_tb t0
        WHERE t0.no = #{num}
    </select>

    <insert id="save" parameterType="boardDto">
        INSERT INTO boardList_tb (title, writer, content, productCd, board_type)
        VALUES (#{title}, #{writer}, #{content}, #{productCd}, #{boardType})
    </insert>

    <insert id="saveVote" parameterType="votedDto">
        INSERT INTO vote_tb VALUES (#{product_cd}, #{user_no})
    </insert>  

    <insert id="viewCount" parameterType="boardDto">
        UPDATE boardList_tb t0
        SET t0.readcnt = t0.readcnt + 1
        WHERE no = #{no}
    </insert>

    <insert id="voteCount" parameterType="cosmeticsDto">
        UPDATE cosmetics_tb 
        SET voteCnt = voteCnt + 1
        WHERE code = #{code}
    </insert>
      
</mapper>