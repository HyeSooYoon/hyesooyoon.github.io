<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sprayou.spray.mapper.BoardMapper">

    <select id="list" resultType="boardDto">
        SELECT no
            , title
            , writer
            , regdte
            , updte
            , readcnt
            , productName
        FROM boardList_tb
    </select>

    <select id="detail" parameterType="String" resultType="boardDto">
        SELECT
        t0.*
        FROM
        boardList_tb t0
        WHERE t0.no = #{num}
    </select>

    <insert id="save" parameterType="boardDto">
        INSERT INTO boardList_tb (title, writer, content, productName)
        VALUES (#{title}, #{writer}, #{content}, #{productName})
    </insert>

    <insert id="viewCount" parameterType="boardDto">
        UPDATE boardList_tb t0
        SET t0.readcnt = t0.readcnt + 1
        WHERE no = #{no}
    </insert>





</mapper>